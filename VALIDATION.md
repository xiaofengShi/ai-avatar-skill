# 验证记录

初次验证：2026-09-21；新增案例：2026-09-22。范围：双入口内容、工具功能、少量虚构人物实测与一组真实旅行照片案例。项目已发布到 [GitHub](https://github.com/xiaofengShi/ai-avatar-skill)。

## 可以确认什么

- ChatGPT prompt 从共享 workflow 自动生成；Codex skill 直接读取相同 workflow。
- Codex 内置图像工具完成了 5 组头像示例：2 组原始照片到头像、1 组相同人物的用途适配、2 组已有头像编辑。仅覆盖 2 个虚构人物。实际 brief 由当前助手按流程编写，不是独立代理盲测或 skill 自动触发测试。
- 另完成 1 组真实旅行照片到插画的委托，共 6 组图像示例。旅行成片获该用户认可并授权公开，其他案例仍是助手检查；这不是六人测试或独立真人盲评。
- 预览工具已验证原图字节保留、内嵌显示、文件名 HTML 转义、拒绝覆盖输出、拒绝覆盖输入和拒绝 SVG 作为图像输入。
- Skill 结构校验通过。结构通过不等于审美或行为测试通过。

## 示例审片

### 真实旅行照片：用户认可

用户先提供街头旅行照片，在用途澄清后要求“先做一个旅行氛围的吧”。使用已安装的 editorial-avatar skill：人物照片提供身份与场景，早期对话画风图提供绘画语言。该画风图未获再分发授权，只在 manifest.json 的 unpublished_inputs 中记录哈希和输入位置；它不是仓库内的公共风格图。此案例与前五组的参考条件不同。

第一次图像调用因 JPEG 的 invalid_image_file 错误未生成结果。将原照转为 PNG 后，用同一份简报调用成功，未作图像质量重试或候选筛选。实际简报见 [generation.prompt.txt](examples/travel/generation.prompt.txt)，人物源图与成片见[项目主页案例区](https://xiaofengshi.github.io/ai-avatar-skill/showcase.html#examples)。公开源图已移除 EXIF、文本等元数据，并核对 PNG 的 IDAT 图像数据未变化。

助手原图检查：侧脸方向、眼镜、短发与背包保留；人物占比提高，路牌、绿荫与柔和日光形成旅行环境肖像。街景、建筑、路牌位置和细节经重构，图中的指路箭头不应用于导航。该图按竖版环境肖像查看，没有作头像圆裁切检查。

用户评价“不错”，并要求将此案例加入 GitHub README，状态为“已生成、助手检查、该用户认可并授权公开”。这是单个真实案例，不能推出身份精确还原、跨人物稳定性或模型优越性。

### 开源社区头像：Codex

输入：虚构男性照片与公共画风参考；用途：GitHub / Hugging Face；保留眼镜。输出为一次生成，没有通过多次抽样挑选。

助手原图审片观察：圆框眼镜、灰白发色、胡茬与宽脸辨识线索保留；墨蓝便装与浅蓝灰背景协调，表情克制。皮肤局部纹理仍偏细碎，绘画简化程度没有参考图那么强。状态为“已生成、助手原图审片”，未获独立真人评价或用户认可。

### 大会演讲者头像：Codex

输入：独立生成的虚构女性照片与公共画风参考；用途：大会演讲者介绍。一次生成，无追加修正。助手原图观察：自然肤色、年龄线索和短卷发保留；梅紫外套、浅灰内搭与灰紫背景形成完整方案。五官略有重绘、头发边缘较细密，不能据此证明身份没有变化。尚未获用户认可。

### 2026-09-22：新增生成与编辑测试

三组首版均一次调用生成；之后微笑与歪头案例收到用户的姿态问题反馈，追加一次修正，详见下文。测试请求由维护助手构造，读取仓库版 SKILL.md 与 workflow.md 后编写简报并调用内置图像工具；没有修改 skill 或共享 prompt。实际输入顺序、请求、简报、输出与哈希均记录在 manifest.json。编辑基底取自公开示例，不代表用户已认可该基底。

| 案例 | 输入与检查重点 | 助手原图观察 | 小尺寸观察 |
| --- | --- | --- | --- |
| 去眼镜 | developer/avatar.png；检查眼镜残留与其他设计变化 | 镜框、鼻托、镜腿已移除；原服装、背景和构图接近保留，眼周及部分面部纹理发生补绘。无真实无眼镜照片作为验证依据。 | 深浅底色的圆裁切均保留发型与面部，40 px 下眼周细节不可细辨。 |
| 深色研究团队头像 | 公共风格图 + developer/source.png；检查用途、年龄特征和深色分离 | 深绿便装、蓝绿背景与柔和侧光落实；眼镜、灰白发丝和胡茬保留。皮肤纹理仍较密集。 | 40 px 下肩部与背景对比偏弱，脸部仍可辨；发型未被圆边裁掉。 |
| 微笑与歪头 | speaker/avatar.png；检查表情、头颈肩及其他设计保留 | 首版曾被助手误判为“头颈关系连贯”；用户指出头与身体方向不协调、观感怪异。该结论已撤回，首版标为用户否定。 | 深浅底色下笑意可见、短卷发未被误裁；40 px 下牙齿与眼部细节不可细辨。 |

三组均在 Codex 内置浏览器实际查看了本地 HTTP 预览的 40、64、128、256 CSS 像素深浅底色圆形裁切。以上是助手观察，未做独立真人评价、身份量化比对或像素级差异分析，不能推出跨人物稳定性。

### 微笑案例的用户反馈与修正

用户指出首版的头和身子方向不协调。助手之前的“头颈关系连贯”评价不准确；小尺寸和圆裁切检查未能发现该整体姿态问题，不能代替审美判断。保留 [首版问题图](examples/speaker-smile/avatar.png) 与原始指令，不将首版继续列为成功案例。

追加一次修正，以原始 speaker/avatar.png 作编辑基底、speaker/source.png 作身份参考；未将用户否定版作为身份参考。修正方向是减小歪头幅度，让头部更接近直立，并连同肩线、领口一起重画。保留微笑、年龄感、服装和配色。实际简报见 [correction.prompt.txt](examples/speaker-smile/correction.prompt.txt)。

[修正版](examples/speaker-smile/avatar-v2.png) 已生成。助手原图观察：头部侧倾减少、双肩更接近正面，颈部与领口过渡相比首版更平缓。已在内置浏览器查看 40、64、128、256 CSS 像素深浅底色圆裁切，头发与脸部未被误裁。此处只描述可见变化；状态为“待用户确认”，没有将生成成功写成用户认可。

### ChatGPT 网页试验（2026-09-21）

在 Chrome 的 ChatGPT 界面实际生成了一张虚构女性照片，未上传私人照片。随后发送完整的首版单消息 workflow 加“用刚刚生成的照片帮我做张头像”，网页直接生成了一张接近修饰照片的头像，没有先询问具体用途。补充技术大会用途后再次得到图像；画风仍偏摄影，未返回要求的独立下载链接。这不是“用途澄清通过”，也不是稳定匹配参考画风的证据。

本次网页操作经历了粘贴无效果、多行模拟键盘输入被拆分、元素 ID 失效等自动化问题；实际发送的首版完整 prompt 经文本框接口输入，界面对 Markdown 添加了转义符。试验还有已生成源图的上下文，不能当作干净的新对话盲测。没有通过分享按钮发布对话或图片。

据此，ChatGPT 入口改为两条消息：先只做方案沟通，再明确“按方案生成”。共享 workflow 也明确了“头像”不等于实际用途。修订版在网页实际完成了方案与生成两步：方案明确引用最初源图，围绕大会用途决定酒红服装、灰蓝背景、自然神态和绘画语言；在收到第二条消息后生成了图片。随后在一个新对话中发送修订版方案阶段加“我想把一张照片做成好看的头像”，实际只收到“这张图主要用在哪里？”，没有生成图像。

状态：两阶段基本交互已验证；风格一致性未证明。最终网页图像在可见尺寸下仍偏写实，绘画感主要体现在背景与衣服，不能称为精确匹配 Codex 参考风格。ChatGPT 没有返回可核验的实际图像工具 prompt、模型版本、seed 或下载文件；网页输出未收入仓库。本文图像示例均来自 Codex 生成记录。ChatGPT 测试使用同一对话中生成的虚构源图，没有测试真实用户上传照片的路径；新对话澄清测试同时缺少用途和照片。

### 圆形裁切与小尺寸

两组 Codex 成片已在 Chrome 页面实际查看 40、64、128、256 CSS 像素、浅色与深色背景的圆形裁切。发型与脸部没有被圆边误切，眼镜与面部轮廓在小图仍可辨认。40 px 下胡茬、细密卷发等纹理不可细辨，这是缩略图的显示限制。没有将这次助手视觉检查写成真人用户认可。

预览开发时曾尝试把大体积图片放进 CSS 自定义属性，实际浏览器中出现空白；已改回普通图像元素并复看通过。仓库样例使用相对引用模式，标准用法仍生成独立内嵌 HTML。

## 当前证据边界

### 已检查与待验证范围

- ChatGPT 真实照片上传路径、双图风格参考与后续编辑；当前只完成虚构源图的基本交互实测。
- 项目主页已在 1280 px 桌面视口与 390 px 手机视口检查：六组案例在桌面为双列网格、手机为单列，页面无横向溢出，原图与结果尺寸相同。12 张案例图片在本地浏览器正常加载；试验记录可用键盘展开。核心内容不依赖 JavaScript。此前头像预览另在 390 CSS 像素 iframe 中检查过；这些不是实体手机测试。打印效果尚未查看。
- 内嵌图像字节与相对路径已通过脚本测试，浏览器视觉检查使用本地 HTTP 页面。尝试打开 file:// 页面时原生浏览器自动化未完成导航，因此直接双击文件的离线显示尚未实测。
- 独立真人审美评价，以及需要时的短 prompt 对照。

没有进行同条件短 prompt 对照、跨真实人物身份验证或多次重复生成，因此不宣称比简短 prompt 更好，也不宣称风格或审美稳定。使用不同入口、人物或有无参考图的样例只能展示覆盖，不能直接比较模型或产品优劣。

## 可复跑的行为测试

在支持图像生成的新对话中复制 [ChatGPT prompt](prompts/chatgpt.md) 的方案阶段，方案明确后发送生成阶段；或在 Codex 调用 skill。使用 [虚构源图](examples/developer/source.png)，避免上传私人照片。

| 测试输入 | 要观察的行为 |
| --- | --- |
| “帮我做张头像。” | 先澄清主要用途，不擅自设为商务风，也不立即生成。 |
| “用于 GitHub 和 Hugging Face，保留眼镜，其余由你设计。” | 简短说明整体设计；ChatGPT 收到第二条生成指令后出图，Codex 连续生成。不重复询问用途，不要求逐项填摄影参数。 |
| “支持去眼镜、歪头、微笑之类的要求。这次先保留眼镜。” | 将前半句视为能力举例，执行保留眼镜，不自动应用三个动作。 |
| “保留这版的构图和服装，只去掉眼镜。” | 使用认可版作编辑基底，检查镜框残留、眼部补绘和身份漂移。 |
| “再瘦 20 斤。” | 作为视觉模拟处理，不宣称精确预测减重结果，不擅自改其他设计。 |

记录原始输入、实际用户消息、能获得的生成指令、入口与日期、每次输出及重试原因。对照试验需要固定图像、用途、模型条件与重试预算；审美评价应盲看同尺寸成片并报告评审人数，不能用助手自评代替独立真人盲审。

## 本地检查命令

```bash
python3 scripts/build_prompt.py --check
python3 -m unittest discover -s tests -v
python3 scripts/check_package.py
npm run check:docs
```

已执行上述 Python 检查（3 项单元测试通过、共享 prompt 同步、28 个素材及指令哈希与本地链接一致）；HTML 由 Markdown 生成并作一致性检查。Skill 另通过 Codex skill-creator 的结构验证。行为测试的观察记录见 [chatgpt-observations.json](tests/chatgpt-observations.json)。
